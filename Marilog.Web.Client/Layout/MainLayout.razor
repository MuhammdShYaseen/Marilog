@using Marilog.Contracts.DTOs.Responses
@using Marilog.Contracts.Interfaces.FrontendServices
@using Marilog.Shared.UI.Layout

@inherits LayoutComponentBase

<SharedMainLayout Items="_items"
                  AppTitle="Marilog"
                  AppSubtitle="WKA Ocean Shipping">

    @Body

</SharedMainLayout>

@code {

    [Inject]
    public INavigationService NavService { get; set; }
        = default!;

    private static List<NavItemResponse>? _items;  // Static cache here
    private static bool _isLoaded = false;
    protected override async Task OnInitializedAsync()
    {
        if (!_isLoaded)
        {
            try
            {
                _items = await NavService.GetAsync();
                _isLoaded = true;
            }
            catch (Exception ex)
            {
                Console.WriteLine(ex);
                _items = new();
            }
        }
    }
    

}