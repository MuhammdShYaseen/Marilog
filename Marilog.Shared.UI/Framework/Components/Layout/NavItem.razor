@inherits FrameworkElement

<div class="@GetCachedClass()"
     style="@GetCachedStyle()"
     @onclick="HandleClick">

    @if (!string.IsNullOrWhiteSpace(Icon))
    {
        <span class="xf-nav-icon">
            <i class="@Icon"></i>
        </span>
    }

    <span class="xf-nav-text">
        @Text
    </span>

</div>

@code {

    [Parameter] public string? Text { get; set; }
    [Parameter] public string? Icon { get; set; }

    [Parameter] public bool IsActive { get; set; }

    [Parameter] public EventCallback<MouseEventArgs> Click { get; set; }

    // =========================
    // CLICK
    // =========================

    private async Task HandleClick(MouseEventArgs args)
    {
        if (!IsEnabled)
            return;

        await Click.InvokeAsync(args);
    }

    // =========================
    // CLASS SYSTEM (FIXED)
    // =========================

    protected override string BuildBaseClass()
    {
        return TailwindHelper.Classes(

            "xf-nav-item",
            "flex items-center gap-2",
            "px-3 py-2 rounded-md",
            "cursor-pointer select-none",

            IsEnabled
                ? "hover:bg-slate-100"
                : "opacity-50 cursor-not-allowed",

            IsActive
                ? "bg-slate-200 font-semibold"
                : "text-slate-700",

            base.BuildBaseClass()
        );
    }

    // =========================
    // STYLE SYSTEM (FIXED)
    // =========================

    protected override string BuildBaseStyle()
    {
        return base.BuildBaseStyle();
    }
}